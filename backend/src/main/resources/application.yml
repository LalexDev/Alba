spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/alba_db
    username: alba_user
    password: Alba123*

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  sql:
    init:
      mode: never

app:
  jwt:
    secret: ${JWT_SECRET:012345678901234567890123456789012345678901234567890123456789}
    expiration-millis: 86400000

server:
  port: 8080